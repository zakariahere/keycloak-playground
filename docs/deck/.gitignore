# Build artefacts — all regenerated by make-deck.ps1, none worth versioning.

# Rendered slides: ~2.5 MB each, 12 per deck.
png/

# The deck itself: ~23 MB, and it is a pure function of the HTML next to it.
*.pptx

# Copied in from the /ppt skill folder on every run. Committing it would fork
# the brand kit -- the source of truth is ~/.claude/skills/ppt/slide-base.css.
slide-base.css
